<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ca.uoit.msohail.charityapp">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true" >
        <activity android:name=".MainActivity" android:theme="@style/AppTheme" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Register" >

        </activity>
        <activity android:name=".CharityOptions" android:theme="@style/AppTheme" >

        </activity>
        <activity android:name=".NearbyCharities" >

        </activity>
        <activity android:name=".ViewAdvertisements">

        </activity>
        <activity android:name=".PostAdvertisement">

        </activity>

    </application>

</manifest>